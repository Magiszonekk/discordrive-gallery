<?xml version="1.0" encoding="utf-8"?>
<!-- importantForAutofill: PIN fields must never talk to the system password
     manager — it was offering to overwrite the DiscorDrive login password
     with the folder PIN (and to autofill the password into the PIN box). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="12dp"
    android:importantForAutofill="noExcludeDescendants">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/pinLayout"
        style="@style/Widget.Gallery.TextInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:endIconMode="password_toggle"
        android:hint="@string/private_unlock_pin_hint">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/pinInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberPassword"
            android:importantForAutofill="no"
            android:textAlignment="center"
            android:letterSpacing="0.4"
            android:maxLength="12"/>
    </com.google.android.material.textfield.TextInputLayout>
</LinearLayout>
